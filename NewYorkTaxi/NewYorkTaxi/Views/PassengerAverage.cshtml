@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model NewYorkTaxi.DisplayModel.AveragePassengerDisplay


@{
    ViewData["Title"] = "View";
}

<h1>Average Data</h1>
<h1>Her skal Det meget gerne stå en graf under!!</h1>
<p>
    @*<a asp-action="Create">Create New</a>*@
</p>

@*@Model.iterator=0;
@foreach (var item in Model.AvgPassengers)
{

    if (Model.iterator < Model.AvgPassengers.Count - 1)
    {
        Model.displaydata = $"['Vendor number {item.VendorID}', {item.PassengerCountAverage}],";
    }
    else
    {
        Model.displaydata = $"['Vendor number {item.VendorID}', {item.PassengerCountAverage}]";
    }
    <text> @Model.displaydata </text>
    @Model.iterator= Model.iterator + 1;
}*@

                ['test',2]

